machine:
  node:
    version: 4

dependencies:
  pre:
    - sudo su - c "curl https://raw.githubusercontent.com/apex/apex/master/install.sh | sh"

deployment:
  prod:
    branch: master
    commands:
    - apex deploy
